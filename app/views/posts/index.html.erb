<div>
	<h1>Index Page</h1>
	<div>
		<%= link_to "New Post", new_post_path %>
	</div>
<div>
<div>
	<% @posts.each do |post| %>
	<div>
		<%= post.title %>
		<br>
		<%= post.text %> <%= link_to "Delete", post, method: :delete, confirm: {data: "are you sure?"} %>
		
	</div>
	<br>
	<% end %>
</div>